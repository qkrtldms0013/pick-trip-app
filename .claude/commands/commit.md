---
description: git-convention.md 규칙에 따라 현재 변경사항을 논리적 단위로 분류하고 커밋합니다.
---

아래 절차에 따라 현재 변경사항을 커밋해 주세요.

> **husky 훅 연동**: 이 저장소는 husky가 적용되어 있어 커밋·푸시 시 아래가 자동 실행됩니다.
> - `pre-commit`: staged 파일에 Biome 검사·자동 수정 후 재스테이징 → **포맷은 자동 처리되므로 별도의 `style` 커밋이 불필요할 수 있습니다.** Biome가 자동 수정할 수 없는 오류가 있으면 커밋이 중단됩니다.
> - `commit-msg`: 커밋 제목이 `<타입>(선택: 범위): <제목>` 형식인지 검증하며, **위반 시 커밋이 거부됩니다.**
> - `pre-push`: `bun run test:run`을 실행하며, 테스트 실패 시 푸시가 중단됩니다.
>
> 훅을 우회하는 `--no-verify` 옵션은 사용하지 마세요.

## 1단계: 변경사항 파악

`git status`와 `git diff`를 실행하여 staged/unstaged 변경사항을 모두 파악하세요.

## 2단계: 논리적 단위로 분류

변경사항을 아래 타입 기준에 맞춰 논리적 단위로 그룹화하세요.
서로 관련 없는 변경사항은 반드시 별도 커밋으로 분리합니다.

| 타입 | 사용 시점 |
|------|----------|
| `feat` | 새로운 기능 추가 |
| `fix` | 버그 수정 |
| `docs` | 문서 변경 (README, 주석 등) |
| `style` | 코드 동작에 영향 없는 포맷 변경 |
| `refactor` | 동작 변경 없는 코드 리팩토링 |
| `chore` | 빌드 스크립트, 패키지 설정 등 잡무 |
| `perf` | 성능 개선 |
| `ci` | CI 설정 또는 스크립트 수정 |
| `release` | 버전 릴리즈 또는 태깅 |

## 3단계: 커밋 실행

각 논리적 단위마다 아래 형식으로 커밋하세요.

**커밋 메시지 형식:**
```
<타입>(선택: 범위): <제목>

(선택) 본문 — 변경 이유와 내용을 간략히

(선택) 푸터 — 이슈 번호, breaking change 등
```

**규칙:**
- 제목의 첫 줄은 반드시 `<타입>(선택: 범위): <제목>` 형식이어야 합니다. (`commit-msg` 훅이 이 형식을 강제하며, 어긋나면 커밋이 거부됩니다.)
- 제목은 한국어로 작성합니다.
- 제목 끝에 마침표를 붙이지 않습니다.
- 민감한 파일(.env, credentials 등)은 절대 커밋하지 않습니다.
- `git add [관련 파일]`로 파일을 선택적으로 스테이징합니다.

**예시:**
```
feat(auth): 카카오 소셜 로그인 기능 추가
fix(trip): 일정 저장 시 날짜 오류 수정
docs: CONTRIBUTING.md 기여 절차 추가
chore: biome 린터 설정 추가
```

## 4단계: 완료 확인

`git log --oneline -10`으로 커밋 이력을 확인하고,
`git status`로 남은 변경사항이 없는지 검증하세요.

$ARGUMENTS
